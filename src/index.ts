import { isString } from "./subchecks/strings/isString";
import {
  maxLength,
  length,
  lengthOfCharacters,
} from "./subchecks/strings/lengths";

const check = {
  isString,
  maxLength,
  length,
  lengthOfCharacters,
};

export default check;
